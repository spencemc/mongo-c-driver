diff --git a/src/libmongoc/src/mongoc/mongoc-cluster.c b/src/libmongoc/src/mongoc/mongoc-cluster.c
index 7c5b18d42..693c20162 100644
--- a/src/libmongoc/src/mongoc/mongoc-cluster.c
+++ b/src/libmongoc/src/mongoc/mongoc-cluster.c
@@ -556,9 +556,14 @@ mongoc_cluster_run_command_monitored (mongoc_cluster_t *cluster,
          cluster, cmd, server_stream->stream, compressor_id, reply, error);
    }
    if (retval && callbacks->succeeded) {
+      /* if this is an unacknowledged write, then generate a fake reply. */
+      bson_t fake_reply = BSON_INITIALIZER;
+      if (!cmd->is_acknowledged) {
+         bson_append_int32 (&fake_reply, "ok", 2, 1);
+      }
       mongoc_apm_command_succeeded_init (&succeeded_event,
                                          bson_get_monotonic_time () - started,
-                                         reply,
+                                         (cmd->is_acknowledged) ? reply : &fake_reply,
                                          cmd->command_name,
                                          request_id,
                                          cmd->operation_id,
@@ -568,6 +573,7 @@ mongoc_cluster_run_command_monitored (mongoc_cluster_t *cluster,
 
       callbacks->succeeded (&succeeded_event);
       mongoc_apm_command_succeeded_cleanup (&succeeded_event);
+      bson_destroy (&fake_reply);
    }
    if (!retval && callbacks->failed) {
       mongoc_apm_command_failed_init (&failed_event,
diff --git a/src/libmongoc/src/mongoc/mongoc-write-command.c b/src/libmongoc/src/mongoc/mongoc-write-command.c
index 4348cdbd4..c096b79ad 100644
--- a/src/libmongoc/src/mongoc/mongoc-write-command.c
+++ b/src/libmongoc/src/mongoc/mongoc-write-command.c
@@ -960,6 +960,7 @@ _mongoc_write_command_execute_idl (mongoc_write_command_t *command,
    }
 
    if (server_stream->sd->max_wire_version >= WIRE_VERSION_OP_MSG) {
+      /* it just might be that OP_MSG doesn't generate a fake reply. */
       _mongoc_write_opmsg (command,
                            client,
                            server_stream,

